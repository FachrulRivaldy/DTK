import 'package:flutter/material.dart';

class PageMatkulDesc extends StatelessWidget {
  const PageMatkulDesc({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
