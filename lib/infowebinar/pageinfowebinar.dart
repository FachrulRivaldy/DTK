import 'package:dtk_database_tekkom/template/headerfooter.dart';
import 'package:flutter/material.dart';

class InfoWebinarTop extends StatelessWidget {
  const InfoWebinarTop({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}

class InfoWebinar extends StatelessWidget {
  const InfoWebinar({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        child: Header(),
      ),
    );
  }
}

class InfoWebinarBot extends StatelessWidget {
  const InfoWebinarBot({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
